@AGENTS.md

## Deploy

Production chạy trên server riêng (lingo.tungtran.dev), không dùng Vercel. Deploy bằng `./deploy.sh`.
Chi tiết server, các app đang chạy, bảng cổng và checklist thêm project mới: @docs/DEPLOY.md
