ANTHROPIC_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=

USER_NAME=test010
PASSWORD=test010@
ADMIN_PASSWORD=admin010@
ADMIN_USER_NAME=admin010

# Ký session cookie — chuỗi ngẫu nhiên dài bất kỳ, đổi sẽ đăng xuất tất cả session cũ.
SESSION_SECRET=

# Tạo database Upstash Redis miễn phí tại https://upstash.com để lưu tiến trình học,
# từ vựng AI sinh thêm, và cấu hình AI provider (bắt buộc để dùng đăng nhập + admin).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=